This folder is only used to store term frequence output files temperarily.
After finish reading it, it'll never be used again, then it should be removed.
So this folder should keep containing only README (this file).
